# Пока не работает потому что все суки хотят за это денег

# name: Deploy to Github Pages

# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# jobs:
#   build-and-deploy:

#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         node-version: [18.x]

#     steps:
#     - name: Checkout 🛎️
#       uses: actions/checkout@v3

#     - name: Install and Build 🛠
#       uses: actions/setup-node@v3
#       with:
#         node-version: ${{ matrix.node-version }}
#         cache: 'npm'
#     - run: npm ci
#     - run: npm run build
#     - run: npm run export

#     - name: Deploy 🚀
#       uses: JamesIves/github-pages-deploy-action@v4
#       with:
#         folder: out
